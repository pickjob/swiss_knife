// Model - contains the data and its structure. There are several QML types for creating models.
// View - a container that displays the data. The view might display the data in a list or a grid.
// Delegate - dictates how the data should appear in the view. The delegate takes each data in the model and encapsulates it.

// import QtQml.Models 
//      ListModel
//          count
//          append(), insert(), move(), remove(), set()
//          ListElement
//      ObjectModel 

// import QtQuick
//      Repeater:
//          count
//          model
//          delegate:
//              index, modelData, named properties